# Lab 8 - Done when

- [ ] The run without healing under `drift_and_bug` had failing tests.
- [ ] You had a healing report: `results/heal/heal_report.html` from your own run, or the recorded one.
- [ ] Every heal in it is marked accept, reject or investigate, with a reason.
- [ ] For every test still red with healing on, you can say whether its locator healed and why it still fails.
- [ ] The healing run changed no test file: `git status -- tests resources` shows the same before and after step 3.
- [ ] `uv run --no-sync python -m shop status` shows `clean`.
