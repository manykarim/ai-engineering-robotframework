# Lab 0 - Done when

- [ ] `git remote -v` shows your fork as `origin`.
- [ ] `uv run --no-sync python setup-check/check.py` reports no red line.
- [ ] `uv run --no-sync python -m shop status` shows the preset `clean` (and your space, if you use the shared
      instance).
- [ ] `uv run robotcode results summary` shows 13 tests: 11 passed, 2 failed.
- [ ] `uv run robotcode results show --failed` lists exactly two tests, and adding `--include broken` lists the same
      two.
- [ ] Your coding agent starts in the repository and answers a question about it.
